import 'package:flutter/material.dart';

class DonorRegistrationDetails extends StatelessWidget {
  const DonorRegistrationDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
